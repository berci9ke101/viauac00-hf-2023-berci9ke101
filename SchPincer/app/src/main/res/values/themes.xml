<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.SchPincer" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SchPincer" parent="Base.Theme.SchPincer" />

    <style name="Theme.SchPincer.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_pincer</item>
        <item name="postSplashScreenTheme">@style/Theme.SchPincer</item>
    </style>

    <style name="ThemeOverlay.SchPincer.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_600</item>
        <item name="fullscreenTextColor">@color/light_blue_A200</item>
    </style>

</resources>